services:
  postgres:
    image: postgres:16-alpine
    container_name: itrms-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-itrms}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-itrms}
      POSTGRES_DB: ${POSTGRES_DB:-itrms}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-itrms}']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
